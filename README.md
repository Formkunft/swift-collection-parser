# Swift Collection Parser

The functionality of this package has been integrated into the [swift-parsing](https://github.com/Formkunft/swift-parsing) package.
Use the `CollectionParsing` target from swift-parsing instead; this package is no longer maintained.
